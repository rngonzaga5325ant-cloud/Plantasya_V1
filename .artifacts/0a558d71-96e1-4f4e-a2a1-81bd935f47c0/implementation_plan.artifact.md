# Fix TensorFlow Lite Metadata build error

The build is failing because the `org.tensorflow.lite.support.metadata` package is missing. This package is required by the code generated by Android Studio's ML Model Binding feature (`mlModelBinding = true`).

## Proposed Changes

### Build Configuration

#### [MODIFY] [libs.versions.toml](file:///C:/Users/PC/AndroidStudioProjects/Plantasya_V1/gradle/libs.versions.toml)
- Update `tensorflowLite` version to `2.17.0`.
- Update `tensorflowLiteSupport` version to `0.5.0`.
- Add `tensorflow-lite-metadata` library definition.

#### [MODIFY] [app/build.gradle.kts](file:///C:/Users/PC/AndroidStudioProjects/Plantasya_V1/app/build.gradle.kts)
- Add `libs.tensorflow.lite.metadata` to the dependencies.
- Ensure `aaptOptions` are configured to not compress `.tflite` files (as a precaution, though usually handled by AGP).

## Verification Plan

### Automated Tests
- Run `./gradlew :app:compileDebugJavaWithJavac` to verify the error is resolved.
- Perform a Gradle Sync to ensure dependencies are correctly resolved.

### Manual Verification
- Check if `ModelUnquant.java` compiles successfully.
