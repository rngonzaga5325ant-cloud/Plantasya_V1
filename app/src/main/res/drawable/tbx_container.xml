<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fill color -->
    <solid android:color="#F7F1DE" />

    <!-- Border stroke: black, width 2dp -->
    <stroke
        android:width="1dp"
        android:color="#000000" />

    <!-- Optional: rounded corners (adjust as needed) -->
    <corners android:radius="4dp" />

    <!-- Optional: padding inside the EditText -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp" />

</shape>